using Avalonia.Controls;

namespace One.Toolbox.Views.HashTool
{
    public partial class HashToolPage : UserControl
    {

        public HashToolPage()
        {
            // DataContext = ViewModel = App.Current.Services.GetService<DashboardVM>();
            InitializeComponent();
        }
    }
}